package enumswitch;

public enum Week {
    //enumerate data type: enum keyword
    //what if you want to force the user to only use, mon, tues, etc
    //enum are all uppercase
    MON, TUES, WED, THU, FRI, SAT, SUN
}
